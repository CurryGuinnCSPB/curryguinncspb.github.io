---
title: "Recipe Generation with GPT-2"
layout: single
permalink: /student-projects/nlp/language-modeling-generation/recipe-generation-gpt2/
excerpt: "Generates cooking recipes using fine-tuned GPT-2 models."
image: /assets/images/student-projects/nlp/recipe-generation-gpt2/hero.jpg
toc: false
---

## Overview

This project focuses on generates cooking recipes using fine-tuned GPT-2 models.

Recipe Generation With GPT-2 & Pythia-160M Emily Henderson CSPB 3832 (ignore the github link that was here in the video I turned in a zip file) Introduction and Background ● If language models could produce quality recipes it could create a valuable tool in many situations (health, efficiency and enjoyment). ● Recipe generation is one of many fields that require context and background knowledge in order to craft a quality results.

---

## Data and System Design

The project draws on X, RecipeNLG Dataset.

The core system is built around GPT-2, transformer, PyTorch. The model combines contextual language modeling with domain-specific signals so that structured entities are easier to detect.

The implementation uses ROUGE.

---

## Results

The project reports qualitative or comparative results rather than a single headline metric. The results point to a workable solution for the target task and a clear path for refinement.

---

## Why This Matters

Generation projects are useful because they show how language models can move from pattern recognition to actual output creation. This project also makes clear that dataset design and prompting choices shape the final result.
